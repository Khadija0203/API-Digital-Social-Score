# ================================================
# Configuration API Toxic Detection
# ================================================
# Copier ce fichier en .env et remplir les valeurs

# === JWT Authentication ===
# Secret pour signer les tokens JWT (utilisez un secret fort en production)
# Générer avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-super-secret-key-change-me-256-bits-minimum

# Algorithme de signature JWT (ne pas modifier)
JWT_ALGORITHM=HS256

# Durée de validité du token en heures
JWT_EXPIRATION_HOURS=24

# === Google Cloud Platform ===
# ID de votre projet GCP
PROJECT_ID=simplifia-hackathon

# Zone du cluster GKE
CLUSTER_ZONE=europe-west1-b

# Nom du cluster GKE
CLUSTER_NAME=mlops-toxic-detection-cluster

# === Cloud Storage ===
# Buckets GCS (générés automatiquement avec PROJECT_ID)
# gs://mlops-models-${PROJECT_ID}
# gs://mlops-data-${PROJECT_ID}

# === MLflow Configuration ===
# URI de tracking MLflow (local par défaut)
MLFLOW_TRACKING_URI=./mlruns

# Nom de l'expérience MLflow
MLFLOW_EXPERIMENT_NAME=toxic-detection

# === API Configuration ===
# Port d'écoute de l'API
PORT=8080

# Activer les métriques Prometheus
ENABLE_METRICS=true

# === Credentials (Production) ===
# Chemin vers le fichier de credentials GCP (optionnel si gcloud configuré)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# === Monitoring (Optionnel) ===
# URL Prometheus (si monitoring externe)
# PROMETHEUS_URL=http://localhost:9090

# URL Grafana (si monitoring externe)
# GRAFANA_URL=http://localhost:3000
